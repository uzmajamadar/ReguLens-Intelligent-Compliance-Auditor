- [x] Update Auditor AI page UI so that when `?documentId=` is present, the document dropdown is hidden/locked to only that document (no “All documents” option)
- [ ] Verify navigation flows:
  - [ ] Visiting `/auditor-ai` shows “All documents” and all conversation history
  - [ ] Visiting `/auditor-ai?documentId=X` shows only document X and its conversation history
- [ ] Run frontend dev/build/test (build already run: `vite build`)

